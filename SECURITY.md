# 安全策略 (Security Policy)

IntraMind AI 重视安全与隐私。本文件说明如何报告安全漏洞以及项目的安全承诺。

## 支持版本

| 版本 | 支持状态 |
|------|---------|
| v1.1.x | ✅ 积极维护 |
| v1.0.x | ⚠️ 仅安全修复 |
| < v1.0.0 | ❌ 不再支持 |

## 报告漏洞

- 请勿在公开 Issue 中披露漏洞细节，避免影响其他使用者
- 请通过 GitHub Issues（标记 security）或仓库维护者邮箱私密反馈
- 报告中请包含：影响版本、复现步骤、预期影响、建议修复（如有）

**响应承诺**

- 48 小时内确认收到报告
- 确认有效后 7 天内发布修复版本或缓解方案

## 安全设计

- **本地化部署**：数据默认保留在内网，不经第三方 SaaS 中转
- **多租户隔离**：TenantContext 贯穿请求链路，知识库数据按租户隔离
- **RBAC 权限**：企业版支持组织 / 工作区 / 角色 / 数据权限四级管控
- **敏感拦截**：企业版支持敏感内容拦截、引用强制与审计日志
- **依赖安全**：CI 内置依赖检查，建议定期执行 `mvn dependency-check` 或等价扫描

## 安全更新建议

- 及时拉取最新 release 并应用补丁
- 生产环境使用强密码，限制数据库 / 对象存储 / Redis 的访问来源
- 开启审计日志并对接企业统一日志平台
- 定期轮换 API Key 与模型服务凭证
